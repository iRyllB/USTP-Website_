# Frontend Environment Variables (exposed to browser - safe to expose)
REACT_APP_SUPABASE_URL=https://your-project-url.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key

# Server-side Environment Variables (NOT exposed to browser)
SUPABASE_URL=https://your-project-url.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key

# API Keys (Server-side only - NEVER expose to browser)
GEMINI_API_KEY=your-gemini-api-key
TINYMCE_API_KEY=your-tinymce-api-key

# SECURITY NOTES:
# - SUPABASE_SERVICE_KEY: Has full database access, bypasses RLS - CRITICAL to keep server-side only
# - GEMINI_API_KEY: Access to Gemini API - should be server-side only
# - TINYMCE_API_KEY: Access to TinyMCE Cloud - should be server-side only
# - Get service key from: Supabase Dashboard > Settings > API > service_role key